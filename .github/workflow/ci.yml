name: CI pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  runs_on: self-hosted
  permission:
    contents: read

  build:
    steps:
      - name: build
        run: echo "hello"

  test:
    steps:
      - name: test
        run: whoami